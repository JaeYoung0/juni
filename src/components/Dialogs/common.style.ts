import styled from '@emotion/styled'

export const Dialog = styled.dialog`
  // dialog position default: fixed

  width: 100%;
  height: 100%;
  padding: 2rem;
  border-radius: 5px;
  overflow: visible;
  border: none;
  background: #000;
`
